task_name: "dictator_game"

llm:    # 进化算法所选用的 llm 类型
  model: "deepseek-v3-250324"
  max_tokens: 16384
  timeout: 6000

evolution_setting:    # 进化算法迭代的参数设置
  max_iterations: 100
  program_pool_size: 20

cache:
  enabled: true
  cache_dir: "__cache__"

evaluation_timeout: 12000   # 完整的单次评估的最大时间