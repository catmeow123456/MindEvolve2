task_name: "trust_game"

llm:
  provider: "litellm"
  model: "gemini/gemini-2.5-pro"
  max_tokens: 10000
  timeout_sec: 600

evolution_setting:
  max_iterations: 10
  program_pool_size: 1

cache:
  enabled: true
  cache_dir: "__cache__"

evaluation_timeout_sec: 6000   # 完整的单次评估的最大时间
seed: 12345