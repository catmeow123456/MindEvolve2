task_name: "trust_game"

llm:
  provider: "litellm"
  model: "gemini/gemini-2.5-flash"
  max_tokens: 10000
  timeout: 6000

evolution_setting:
  max_iterations: 4
  program_pool_size: 2

cache:
  enabled: true
  cache_dir: "__cache__"

evaluation_timeout: 120000   # 完整的单次评估的最大时间
seed: 12345