task_name: "trust_game"

llm:
  provider: "litellm"
  model: "gemini/gemini-2.5-flash"
  max_tokens: 10000
  timeout_sec: 6000

evolution_setting:
  max_iterations: 2
  program_pool_size: 4

cache:
  enabled: true
  cache_dir: "__cache__"

evaluation_timeout_sec: 120000   # 完整的单次评估的最大时间
seed: 12345